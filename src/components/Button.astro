---
interface ButtonProps {
  text: string;
  styles?: string;
}

const { text, styles } = Astro.props as ButtonProps;
---

<button
  class=`w-full text-lg md:text-xl px-8 py-4 bg-dark text-white rounded-xl md:hover:text-dark md:hover:bg-primary ${styles}`
>
  {text}
</button>
